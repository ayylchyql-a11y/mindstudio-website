const $$=(s,r=document)=>[...r.querySelectorAll(s)],$=(s,r=document)=>r.querySelector(s);let mode='Consegna',cart=[];const header=$('[data-header]'),drawer=$('[data-cart]'),scrim=$('[data-scrim]'),toast=$('[data-toast]');
addEventListener('scroll',()=>header.classList.toggle('fixed',scrollY>65),{passive:true});
const setMode=value=>{mode=value;$$('[data-mode]').forEach(b=>{let on=b.dataset.mode===value;b.classList.toggle('active',on);b.setAttribute('aria-pressed',on)});$('[data-mode-label]').textContent=value;$('[data-cart-mode]').textContent=value;$('[data-mode-note]').textContent=value==='Consegna'?'25–35 min · da € 2,50':'Pronto in 20 min · gratuito'};
$$('[data-mode]').forEach(b=>b.onclick=()=>setMode(b.dataset.mode));$('[data-mode-direct]').onclick=()=>{setMode('Ritiro');location.hash='menu'};$('[data-change-mode]').onclick=()=>setMode(mode==='Consegna'?'Ritiro':'Consegna');
const nav=$('[data-mobile-nav]'),navBtn=$('[data-menu-button]');navBtn.onclick=()=>{let open=navBtn.getAttribute('aria-expanded')!=='true';navBtn.setAttribute('aria-expanded',open);navBtn.setAttribute('aria-label',open?'Chiudi il menu':'Apri il menu');nav.classList.toggle('open',open);nav.setAttribute('aria-hidden',!open);document.body.classList.toggle('lock',open)};$$('[data-nav-link]').forEach(a=>a.onclick=()=>navBtn.click());
$$('[data-filter]').forEach(b=>b.onclick=()=>{$$('[data-filter]').forEach(x=>{let on=x===b;x.classList.toggle('active',on);x.setAttribute('aria-pressed',on)});$$('[data-category]').forEach(p=>p.classList.toggle('hidden',b.dataset.filter!=='tutti'&&!p.dataset.category.split(' ').includes(b.dataset.filter)))});
function render(){let count=cart.length,total=cart.reduce((s,x)=>s+x.price,0);$('[data-cart-count]').textContent=count;$('[data-open-cart]').setAttribute('aria-label',`Apri carrello, ${count} articoli`);$('[data-cart-total]').textContent=`€ ${total.toFixed(2).replace('.',',')}`;$('[data-checkout]').disabled=!count;$('[data-cart-items]').innerHTML=count?cart.map((x,i)=>`<div class="cart-item"><span>${x.name}</span><b>€ ${x.price.toFixed(2).replace('.',',')}</b><button aria-label="Rimuovi ${x.name}" data-remove="${i}">×</button></div>`).join(''):'<div class="empty"><span>□</span><h3>La box è ancora vuota</h3><p>Aggiungi qualcosa dal menu per iniziare.</p></div>';$$('[data-remove]').forEach(b=>b.onclick=()=>{cart.splice(+b.dataset.remove,1);render()})}
$$('[data-add]').forEach(b=>b.onclick=()=>{cart.push({name:b.dataset.name,price:+b.dataset.price});render();toast.classList.add('show');clearTimeout(toast._t);toast._t=setTimeout(()=>toast.classList.remove('show'),1800)});
const openCart=()=>{drawer.classList.add('open');drawer.setAttribute('aria-hidden','false');scrim.classList.add('show');document.body.classList.add('lock')},closeCart=()=>{drawer.classList.remove('open');drawer.setAttribute('aria-hidden','true');scrim.classList.remove('show');document.body.classList.remove('lock')};$$('[data-open-cart]').forEach(b=>b.onclick=openCart);$('[data-close-cart]').onclick=closeCart;scrim.onclick=closeCart;$('[data-checkout]').onclick=()=>{toast.textContent='✓ Demo ordine completata';toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2200)};
const items=$$('.reveal');if(matchMedia('(prefers-reduced-motion:reduce)').matches)items.forEach(x=>x.classList.add('show'));else{let io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('show');io.unobserve(e.target)}}),{threshold:.08});items.forEach(x=>io.observe(x))}render();

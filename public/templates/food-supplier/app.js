const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];let request=[];const header=$('[data-header]'),drawer=$('[data-drawer]'),scrim=$('[data-scrim]'),toast=$('[data-toast]');addEventListener('scroll',()=>header.classList.toggle('fixed',scrollY>70),{passive:true});const menu=$('[data-menu]'),mobile=$('[data-mobile]');menu.onclick=()=>{let open=menu.getAttribute('aria-expanded')!=='true';menu.setAttribute('aria-expanded',open);menu.setAttribute('aria-label',open?'Chiudi il menu':'Apri il menu');mobile.classList.toggle('open',open);mobile.setAttribute('aria-hidden',!open);document.body.classList.toggle('lock',open)};$$('[data-link]').forEach(a=>a.onclick=()=>menu.click());
$$('[data-filter]').forEach(b=>b.onclick=()=>{$$('[data-filter]').forEach(x=>{let on=x===b;x.classList.toggle('active',on);x.setAttribute('aria-pressed',on)});$$('[data-category]').forEach(x=>x.classList.toggle('hidden',b.dataset.filter!=='tutti'&&!x.dataset.category.split(' ').includes(b.dataset.filter)))});
function render(){$('[data-count]').textContent=request.length;$('[data-items]').innerHTML=request.length?request.map((x,i)=>`<div class="request-item"><span>${x}</span><button aria-label="Rimuovi ${x}" data-remove="${i}">×</button></div>`).join(''):'<p class="empty">Aggiungi prodotti dal catalogo oppure inviaci una richiesta generale.</p>';$$('[data-remove]').forEach(b=>b.onclick=()=>{request.splice(+b.dataset.remove,1);render()})}$$('[data-add]').forEach(b=>b.onclick=()=>{if(!request.includes(b.dataset.name))request.push(b.dataset.name);render();toast.textContent='✓ Prodotto aggiunto alla richiesta';toast.classList.add('show');clearTimeout(toast.t);toast.t=setTimeout(()=>toast.classList.remove('show'),1700)});
const open=()=>{if(menu.getAttribute('aria-expanded')==='true')menu.click();drawer.classList.add('open');drawer.setAttribute('aria-hidden','false');scrim.classList.add('show');document.body.classList.add('lock')},close=()=>{drawer.classList.remove('open');drawer.setAttribute('aria-hidden','true');scrim.classList.remove('show');document.body.classList.remove('lock')};$$('[data-open-quote]').forEach(b=>b.onclick=open);$('[data-close]').onclick=close;scrim.onclick=close;$('[data-form]').onsubmit=e=>{e.preventDefault();close();e.target.reset();request=[];render();toast.textContent='✓ Richiesta inviata · Risposta entro 1 giorno';toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),3500)};
$('[data-postcode-form]').onsubmit=e=>{e.preventDefault();let cap=$('#cap').value;let ok=/^(40|41|42|43)/.test(cap);$('[data-postcode-result]').textContent=ok?'✓ Zona servita — contattaci per il giorno di consegna':'Il CAP non è ancora coperto. Scrivici: valuteremo una soluzione.'};let items=$$('.reveal');if(matchMedia('(prefers-reduced-motion:reduce)').matches)items.forEach(x=>x.classList.add('show'));else{let io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('show');io.unobserve(e.target)}}),{threshold:.08});items.forEach(x=>io.observe(x))}render();

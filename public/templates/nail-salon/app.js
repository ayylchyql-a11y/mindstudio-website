const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];const header=$('[data-header]'),menu=$('[data-menu]'),mobile=$('[data-mobile]'),dialog=$('[data-dialog]'),form=$('[data-form]'),toast=$('[data-toast]');addEventListener('scroll',()=>header.classList.toggle('fixed',scrollY>70),{passive:true});
menu.onclick=()=>{let open=menu.getAttribute('aria-expanded')!=='true';menu.setAttribute('aria-expanded',open);menu.setAttribute('aria-label',open?'Chiudi il menu':'Apri il menu');mobile.classList.toggle('open',open);mobile.setAttribute('aria-hidden',!open);document.body.classList.toggle('lock',open)};$$('[data-link]').forEach(x=>x.onclick=()=>menu.click());
$$('[data-mood]').forEach(b=>b.onclick=()=>{$$('[data-mood]').forEach(x=>{let on=x===b;x.classList.toggle('active',on);x.setAttribute('aria-pressed',on)});$$('[data-look]').forEach(x=>x.classList.toggle('hidden',b.dataset.mood!=='tutti'&&x.dataset.look!==b.dataset.mood))});
$$('[data-book]').forEach(b=>b.onclick=()=>{if(menu.getAttribute('aria-expanded')==='true')menu.click();dialog.showModal();let select=$('[data-service-select]');if(b.dataset.service)select.value=b.dataset.service;let d=dialog.querySelector('input[type=date]');d.min=new Date(Date.now()+86400000).toISOString().split('T')[0]});$('[data-close]').onclick=()=>dialog.close();dialog.onclick=e=>{let r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close()};form.onsubmit=e=>{e.preventDefault();dialog.close();form.reset();toast.classList.add('show');clearTimeout(toast.t);toast.t=setTimeout(()=>toast.classList.remove('show'),3500)};
let items=$$('.reveal');if(matchMedia('(prefers-reduced-motion:reduce)').matches)items.forEach(x=>x.classList.add('show'));else{let io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('show');io.unobserve(e.target)}}),{threshold:.08});items.forEach(x=>io.observe(x))}
